Documents metadata JSON files live here. Each file is named `<doc-id>.json` and includes fields like `id`, `name`, `type`, `size`, `uploadedAt`, `category`, `tags`, `unitUic`, `subject`, `dueDate`, `notes`, `uploadedById`, `currentStage`, and optional `requestId` linking back to a request.

