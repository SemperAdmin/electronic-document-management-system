Requests metadata JSON files live here. Each file is named `req-<timestamp>.json` and includes fields like `id`, `subject`, `dueDate`, `notes`, `unitUic`, `uploadedById`, `documentIds`, `createdAt`, `currentStage`.

